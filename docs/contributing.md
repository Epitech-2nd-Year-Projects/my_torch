# Contributing

## Formatting and linting
- Install dev tools: `python -m pip install -e .[dev]`
- Format code with Black: `python -m black .`
- Lint (and optionally autofix) with Ruff: `python -m ruff check .` or `python -m ruff check --fix .`
